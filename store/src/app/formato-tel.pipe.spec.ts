import { FormatoTelPipe } from './formato-tel.pipe';

describe('FormatoTelPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoTelPipe();
    expect(pipe).toBeTruthy();
  });
});
